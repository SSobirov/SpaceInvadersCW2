# Requirements

- JDK 8+ (JavaFX included in JDK 8; for newer JDKs install JavaFX separately)
- FXGL library on the classpath
- IDE recommended (IntelliJ IDEA or Eclipse)

This repository does not include a build tool configuration. You may add Maven/Gradle if you want reproducible builds.
